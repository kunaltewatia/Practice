<%= form_for :subscription, url: "/customers/#{params[:customer_id]}/subscriptions/#{params[:id]}", remote: true, method: :put do |f| %>
  <%= render partial: 'subscription_form', locals: { f: f } %>
<% end %>